[project]
name = "mem0-server"
version = "0.1.0"
description = "MCP Server for Mem0AI"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.11.0",
    "mem0ai>=0.1.117",
    "pydantic>=2.11.7",
]

[tool.setuptools]
packages = ["models", "services"]
